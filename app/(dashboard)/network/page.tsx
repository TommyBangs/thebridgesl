import NetworkPageClient from "./layout"

export const metadata = {
  title: "Network",
  description: "Connect with peers, alumni, and industry professionals",
}

export default function NetworkPage() {
  return <NetworkPageClient />
}
